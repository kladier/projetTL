Les chiffres correspondent à l'ordre de priorité : 
1 -> a faire en premier
2 -> a faire avant le rendu
3 -> a faire éventuellement si on a plus rien à faire (donc pas grave si c'est pas fait)

On peux marquer comme ca quand c'est fait : 1) [OK] blablabla
	
	1) finir la standardisation (un bug a réglé et quelques retouches à faire)
	
	1) s'occuper des bugs du bouton Suivant 
	
	1) S'occuper du bouton Précédent (pas encore fait)
	
	1) finir la documentation : seuls les classes automate, etat et mainwindow ont une doc, reste à faire les autres qui concernent en fait uniquement les affichages utilisées pour la création d'un automate. Il faut ajouter le depot git du projet et expliquez très vite fait comment y accéder (renvoyer un lien vers un tuto git)
	
	1) commencer a faire sérieusement le compte-rendu !
		
	2) voir pour les exécutables, il faudrait dans l'idéal avoir un "protocole" pour générer les exécutables pour les différents OS (Linux/Window)
		Il faut surtout arriver à faire ces exécutables.
	
	
	2) possibilité d'agrandir / réduire les différents blocs (bloc texte à droite, automate initial et automate final)
	
	2) problème repéré quand on fait plusieurs opérations sans fermer l'UI, par exemple : si on fait une minimisation puis une déterminisation, le bouton suivant nous renvoit vers la minimisation
	
	2) boutons standardiser & minimiser en png (permet de mettre aucun fond dans les images au lieu du blanc)
	
	2) pas afficher les boutons standardiser et minimiser dès le démarrage mais seulement quand il y a un automate
		d'une manière générale, revoir l'affichage des boutons dans les différents cas : quand on fait le produit, pas possible d'afficher ce bouton
	
	2) pour la minimisation, il faut afficher un peu mieux le tableau, avec E0 E1 ... en haut pour état 0 état 1 ...
		B0 (bilan 0) et pour les lignes concernant les transitions mettre "en 1" (pour la ligne concernant les transitions étiquettées avec le num 1
	

	2) empêcher la minimisation pour les automates qui ne sont pas standard et déterministe
	
	2) faire en sorte qu'il soit simple d'ajouter une fonctionnalité sur les automates dans le code. Détailler comment faire dans la doc (page principale)
	
	3) dans l'idéal il faudrait revoir un peu l'UI, càd faire 2 grosses colonnes pour l'automate initial (à gauche par exemple) et l'automate minimisé ou déterminisé à droite. Faire un bloc qui prend toute la longueur de la page en bas et permettant d'afficher le texte. 
	D'une manière générale, réfléchir à ce que l'affichage soit mieux fait : on voit rarement correctement l'automate inital quand on fait une opération
	
	3) empêcher d'enregistrer un automate s'il n'est pas conforme à nos normes : - les transitions ont des numéros >= 1 et qui se suivent (1,2,3) et pas (1,3,4)
										    - les numéros des états commencent à 0 et se suivent 
										    - pas plus de 8/10 états par automate
	C'est pas le plus urgent mais ca permettrait d'empêcher beaucoup de bugs et de crash du logiciel.
	

	3) pour toutes les opérations, revoir les textes, faire en sorte que ce soit bien du pas à pas et que les explications soient claires et précises pour des étudiants.
	
	
	3) commenter plus le code, et il y a énormément d'améliorations / d'optimisation qu'on pourrait apporter 
	
	
	3) possibilité d'enregistrer les automates qui ont été généré par une opération
	
