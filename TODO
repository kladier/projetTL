
	
	1) finir le compte-rendu !
		
	 2) Continuer la doc, surtout la page principale : ajouter les définitions, le depot git du projet et expliquez très vite fait comment y accéder (renvoyer un lien vers un tuto git)
	 
	 
	2) voir pour les exécutables, il faudrait dans l'idéal avoir un "protocole" pour générer les exécutables pour les différents OS (Linux/Window)
		Il faut surtout arriver à faire ces exécutables.
	
	
	2) pour la minimisation, il faut afficher un peu mieux le tableau, avec E0 E1 ... en haut pour état 0 état 1 ...
		B0 (bilan 0) et pour les lignes concernant les transitions mettre "en 1" (pour la ligne concernant les transitions étiquettées avec le num 1
	
	
	2) faire en sorte qu'il soit simple d'ajouter une fonctionnalité sur les automates dans le code. Détailler comment faire dans la doc (page principale)
	
	2) si l'automate est déjà minimisée, faire en sorte de ne pas refaire l'opération de minimisation
	
	2) refaire des exemples et en supprimer, faire en sorte que les exemples soient adaptés
	
	2) faire une fonction isComplet() et compléter la minimisation pour pouvoir minimiser des automates déterministes mais non complet
	
	
	pas le temps, demande des changements trop importants : possibilité d'agrandir / réduire les différents blocs (bloc texte à droite, automate initial et automate final)
